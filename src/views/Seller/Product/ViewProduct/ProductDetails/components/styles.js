//   import { makeStyles } from "@mui/styles";

import { createStyles } from "@mui/material";



const auctionPanelStyles = createStyles(theme => ({
    wrapper: {
        height: '20px',
        width: '100%',
        border: '1px solid red'
    }
}))

export default auctionPanelStyles;