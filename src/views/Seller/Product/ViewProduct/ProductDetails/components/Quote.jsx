// import { useTheme } from "@mui/material";

const Quote = ({ children }) => {
  // const theme = useTheme()

  return (
      <span style={{ color: `#20e28e` }}> 
      {children}
      </span>
  );
};

export default Quote;
